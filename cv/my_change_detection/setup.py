from setuptools import setup, find_packages

setup(
    name='my_change_detection_lib',
    version='0.1',
    packages=find_packages(),
    description='A simple change detection library in Python',
    author='iota',
    python_requires='>=3.6',
)
